__pycache__/
*.py[cod]
build/
dist/
.DS_Store
Thumbs.db

# Active development / research / in-progress testing material -- local
# only, never committed. Promote individual pieces into the real project
# (src/, docs/, etc.) and commit those specifically once confirmed/finished;
# don't commit this folder wholesale.
/dev/
.claude/

# The user's own local Saved-Command-Button/Plugin exports, kept for
# personal use -- not shared community content, never committed. See
# community/buttons/ and community/plugins/ for the shared equivalents.
/scripts/
